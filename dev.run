#!/bin/bash

# Development k6 Test Runner
# Runs k6 tests in Docker against dev environment

set -e

echo "========================================="
echo "🧪 Running k6 Tests (Development Mode)"
echo "========================================="
echo ""

# Colors
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m' # No Color

# Check if dev environment is running
echo "📋 Checking if development environment is running..."

if ! curl -s http://localhost:8000 > /dev/null 2>&1; then
    echo -e "${RED}✗ Development environment is not running${NC}"
    echo ""
    echo "Please start the development environment first:"
    echo "  ./dev.sh"
    echo ""
    exit 1
fi

echo -e "${GREEN}✓ Development environment is running${NC}"
echo ""

# Build and run k6 tests
echo "🐳 Building and running k6 tests..."
echo ""

docker-compose -f docker-compose.dev.yml --profile test up --build k6-tests

# Capture exit code
EXIT_CODE=$?

echo ""
if [ $EXIT_CODE -eq 0 ]; then
    echo "========================================="
    echo -e "${GREEN}✅ ALL TESTS PASSED${NC}"
    echo "========================================="
else
    echo "========================================="
    echo -e "${RED}❌ SOME TESTS FAILED${NC}"
    echo "========================================="
fi

echo ""
echo "📝 To view detailed logs:"
echo "  docker logs bw-k6-tests-dev"
echo ""

exit $EXIT_CODE

